import { Props } from './types';

const Icon = ( {  }: Props ) => {
    return (
        <span
            aria-roledescription="Some description"
            role="img"
        >
            
        </span>
    )
};

export default Icon;
