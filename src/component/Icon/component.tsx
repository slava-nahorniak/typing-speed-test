import React from 'react';

export interface Props {
    
};

const Icon = ({  }: Props) => {
    return (
        <span
            aria-roledescription="Some description"
            role="img"
        >
            
        </span>
    )
};

export default Icon;
